{
  "name": "skill-test",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal TypeScript scratch project used to test the om-fix regression-gate flow (PR #94/#95) in action.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "fast-check": "^4.9.0",
    "tsx": "^4.23.13",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
